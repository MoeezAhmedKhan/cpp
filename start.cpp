// Step 1: Install MinGW and add its 'bin' folder to environment variables

// Step 2: Install required VS Code extensions:
// - C/C++
// - Code Runner

// Step 3: Compile the program using:
// g++ start.cpp -o start
// (-o is used to rename the output executable file)

// g++ start.cpp -o start && start.exe (compile and run in a sinle command)

#include<iostream>
using namespace std;

int main(){
    cout << "Hello";
    return 0;
}